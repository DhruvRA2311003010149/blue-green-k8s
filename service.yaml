apiVersion: v1
kind: Service
metadata:
  name: bluegreen-service
spec:
  type: NodePort
  selector:
    app: myapp
    version: blue        # ← This controls which version gets traffic!
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30091
    